---
phase: 04-validation-and-distribution
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: false
requirements:
  - FMT-03
must_haves:
  truths:
    - "CI workflow YAML is syntactically valid"
    - "All workflow steps reference correct paths and identifiers"
    - "The workflow is ready to be pushed and will trigger correctly"
  artifacts: []
  key_links: []
---

<objective>
Validate the complete CI workflow before pushing to GitHub. Verify YAML syntax, path correctness, and ensure the workflow is ready for real execution.

Purpose: Catch configuration errors before they waste CI minutes. One final review before the pipeline goes live.
Output: Validated workflow ready for push
</objective>

<execution_context>
@/Users/jem/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-validation-and-distribution/04-01-SUMMARY.md
@.planning/phases/04-validation-and-distribution/04-02-SUMMARY.md
@.github/workflows/build_and_test.yml
@plugin/CMakeLists.txt
@plugin/packaging/distribution.xml.template
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate workflow YAML and cross-reference paths</name>
  <files></files>
  <action>
Perform automated validation of the CI workflow:

1. **YAML syntax check:** Use `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/build_and_test.yml'))"` to verify YAML is parseable

2. **Path cross-reference:** Verify these references in the workflow match the actual project:
   - `PLUGIN_CODE Tnwd` in CMakeLists.txt matches `aufx Tnwd Manz` in auval step
   - `PLUGIN_MANUFACTURER_CODE Manz` matches auval step
   - `BUNDLE_ID "com.manzanita-research.toneword"` matches pkgbuild identifiers
   - `FORMATS Standalone AU VST3` matches artifact paths in workflow
   - `packaging/distribution.xml.template` exists
   - `packaging/pamplejuce.icns` exists (or update icon reference)
   - `packaging/resources/EULA` and `packaging/resources/README` exist

3. **Working directory consistency:** Ensure all build steps that reference `plugin/` paths use the correct working-directory setting

4. **Fix any issues found** — correct paths, identifiers, or references that don't match

5. **Verify distribution.xml.template** variables match what GitHubENV.cmake exports (PRODUCT_NAME, BUNDLE_ID, VERSION)
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/build_and_test.yml')); print('YAML valid')"</automated>
    <manual>All path references cross-checked against actual project structure</manual>
  </verify>
  <done>Workflow YAML is syntactically valid and all paths/identifiers match the actual project</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final review before pushing CI workflow</name>
  <files>.github/workflows/build_and_test.yml, plugin/packaging/distribution.xml.template</files>
  <action>
Present the complete CI/CD pipeline for human review. What was built:
- GitHub Actions workflow at .github/workflows/build_and_test.yml
- Builds universal binary (arm64+x86_64) on macOS
- Runs Catch2 unit tests via CTest
- Validates VST3 and AU with pluginval (strictness 10)
- Validates AU with auval (aufx Tnwd Manz)
- Conditionally signs, packages as .pkg, and notarizes (when Apple Developer secrets configured)
- Uploads unsigned artifacts as fallback
- Creates GitHub Release on version tag push
- Updated distribution.xml.template (removed CLAP, updated min OS)

How to verify:
1. Review .github/workflows/build_and_test.yml — check that the workflow structure makes sense
2. Review plugin/packaging/distribution.xml.template — confirm CLAP removed, 3 choices remain
3. If ready to test: `git push` to trigger the workflow on GitHub Actions
4. After push: check GitHub Actions tab for the workflow run
5. Verify the build completes and pluginval/auval pass

Note: Code signing and notarization will be skipped until GitHub secrets are configured.
The secrets needed (when Apple Developer account is ready):
- DEV_ID_APP_CERT, DEV_ID_APP_PASSWORD
- DEV_ID_INSTALLER_CERT, DEV_ID_INSTALLER_PASSWORD
- DEVELOPER_ID_APPLICATION, DEVELOPER_ID_INSTALLER
- NOTARIZATION_USERNAME, NOTARIZATION_PASSWORD, TEAM_ID
  </action>
  <verify>User reviews and approves the complete CI/CD pipeline</verify>
  <done>User types "approved" confirming the pipeline is ready for push</done>
</task>

</tasks>

<verification>
- [ ] Workflow YAML parses without errors
- [ ] AU identifiers match CMakeLists.txt (aufx Tnwd Manz)
- [ ] Bundle ID matches across workflow and CMakeLists.txt
- [ ] All referenced files (icons, resources, templates) exist
- [ ] User has reviewed and approved the pipeline
</verification>

<success_criteria>
The complete CI/CD pipeline is validated and approved. Ready for push to GitHub where it will run automatically on the next commit to main or PR.
</success_criteria>

<output>
After completion, create `.planning/phases/04-validation-and-distribution/04-03-SUMMARY.md`
</output>
